program HelloWorld;

var
    newint : integer;
    str: string;

begin
    newint := 10;
    scan(newint);
    scan(str);
    
    if newint == 10 then
        Writeln(str)
    else
        begin
            newint := newint + 5;
            Writeln(newint);
        end;


end.
